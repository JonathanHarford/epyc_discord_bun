{
  "name": "epyc_discord_bun",
  "module": "bot.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "test": "bun docker:up && bunx prisma migrate deploy && bun test --preload ./tests/setup.ts --coverage"
  },
  "dependencies": {
    "@prisma/client": "^5.2.0",
    "discord.js": "^14.13.0",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4"
  }
}